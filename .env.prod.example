# Production Environment Configuration for AcoomH Backend
# Copy this file to .env.prod and update with your production values

# Database Configuration
MYSQL_ROOT_PASSWORD=change_this_to_a_secure_password

# JWT Configuration
# IMPORTANT: Generate a secure random string of at least 64 characters
# You can use: openssl rand -base64 64
JWT_SECRET=change_this_to_a_very_long_and_secure_jwt_secret_key_minimum_64_characters_for_production_security

# File Storage Configuration
UPLOADS_PATH=/var/www/uploads
DOMAIN=api.acoomh.ro

# SSL Configuration (optional, for custom certificates)
SSL_CERT_PATH=./nginx/ssl/cert.pem
SSL_KEY_PATH=./nginx/ssl/key.pem

# Additional Security (optional)
ASPNETCORE_ENVIRONMENT=Production
